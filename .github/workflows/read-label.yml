name: read label

on: 
  pull_request:
    types: [opened, reopened]

jobs:
  read-label:
    runs-on: ubuntu-latest
    steps:
      - run: echo "PR label is `${{ fromJson(github.event.pull_request.labels) }}`"
